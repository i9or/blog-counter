{
  "name": "blog-counter",
  "version": "0.1.1",
  "private": true,
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "watch": "bun --watch src/index.ts",
    "build": "bun build --compile --minify --sourcemap src/index.ts --outfile dist/blog-counter"
  },
  "devDependencies": {
    "bun-types": "1.1.21",
    "prettier": "3.3.3"
  },
  "peerDependencies": {
    "typescript": "5.5.4"
  }
}
